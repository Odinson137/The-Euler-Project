
"""
Сумма простых чисел меньше 10 равна 2 + 3 + 5 + 7 = 17.

Найдите сумму всех простых чисел меньше двух миллионов.
"""



def just_number(max_number):
    sums = 17
    for n in range(11,max_number): # сумма первых 10 нам уже известна, поэтому их можно пропустить. А вообще этот код закрывает одну проблему, которую я никак не могу решить
            # необязательная строчка \|/, но зато точно показывает, какие числа называются простыми
            if n / 1 == n and n / n == 1 and\
                n % 3 == 0 or n % 2 == 0 or\
                n % 4 == 0 or n % 5 == 0 or\
                n % 6 == 0 or n % 7 == 0 or\
                n % 8 == 0 or n % 9 == 0:
                pass

            # TODO Проблема: она заключается в том, что мой код не может найти простые числа в диапозоне от 1 до 10.
            # TODO В будущем надо будет найти новый способ
                
            else:
                sums += n
    return sums

print(just_number(2000000))
